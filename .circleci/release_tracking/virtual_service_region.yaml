apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: frontend-virtual-service
spec:
  hosts:
    - "dev.${REGION}.circleci-labs.com"
  gateways:
    - istio-ingress/${REGION}-istio-gateway-2